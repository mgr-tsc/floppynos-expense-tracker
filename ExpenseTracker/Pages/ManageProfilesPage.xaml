<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:ExpenseTracker.PageModels"
             xmlns:models="clr-namespace:ExpenseTracker.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:DataType="pageModels:ManageProfilesPageModel"
             x:Class="ExpenseTracker.Pages.ManageProfilesPage"
             x:Name="ProfilesPage"
             Title="Settings">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference ProfilesPage}, x:DataType=ContentPage}"
                EventName="Appearing"
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Reset App" Command="{Binding ResetCommand}" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Spacing="{StaticResource LayoutSpacing}" Padding="{StaticResource LayoutPadding}">
            <Label Text="Profiles" Style="{StaticResource Title2}"
                   SemanticProperties.HeadingLevel="Level1" />

            <VerticalStackLayout Spacing="{StaticResource LayoutSpacing}"
                BindableLayout.ItemsSource="{Binding Profiles}">
                <!-- <BindableLayout.ItemTemplate> -->
                <!--     <DataTemplate x:DataType="models:Profile"> -->
                <!--         <Border> -->
                <!--             <Grid ColumnDefinitions="Auto,*" ColumnSpacing="15" Padding="5"> -->
                <!--                 <Label Text="Name" -->
                <!--                        Grid.Column="0" -->
                <!--                        VerticalOptions="Center" -->
                <!--                        Style="{StaticResource Body1Strong}" /> -->
                <!--                 <Entry Text="{Binding Name}" -->
                <!--                        Grid.Column="1" -->
                <!--                        SemanticProperties.Description="{Binding Name, StringFormat='{0} profile name'}" /> -->
                <!--             </Grid> -->
                <!--         </Border> -->
                <!--     </DataTemplate> -->
                <!-- </BindableLayout.ItemTemplate> -->
            </VerticalStackLayout>

            <Button Text="Save Profiles"
                    Command="{Binding SaveProfilesCommand}"
                    MinimumHeightRequest="{OnIdiom 44, Desktop=60}"
                    Margin="0,10"
                    SemanticProperties.Description="Save profile names" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
