<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:ExpenseTracker.PageModels"
             xmlns:models="clr-namespace:ExpenseTracker.Models.Supabase"
             xmlns:controls="clr-namespace:ExpenseTracker.Pages.Controls"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.SegmentedControl;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:fonts="clr-namespace:ExpenseTracker.Resources.Fonts"
             x:Class="ExpenseTracker.Pages.MainPage"
             x:DataType="pageModels:MainPageModel"
             x:Name="OverviewPage"
             Title="{Binding Today}">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="NavigatedTo"
                Command="{Binding NavigatedToCommand}" />
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="NavigatedFrom"
                Command="{Binding NavigatedFromCommand}" />
        <toolkit:EventToCommandBehavior
                BindingContext="{Binding Path=BindingContext, Source={x:Reference OverviewPage}, x:DataType=ContentPage}"
                EventName="Appearing"
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid RowDefinitions="*,Auto">
        <!-- Main Content Area -->
        <Grid Grid.Row="0">
            <pullToRefresh:SfPullToRefresh
                IsRefreshing="{Binding IsRefreshing}"
                RefreshCommand="{Binding RefreshCommand}">
                <pullToRefresh:SfPullToRefresh.PullableContent>
                    <ScrollView>
                        <Grid Padding="{StaticResource LayoutPadding}" 
                              RowSpacing="{StaticResource LayoutSpacing}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <controls:HeaderBar Grid.Row="0" UserName="{Binding UserName}" ></controls:HeaderBar>
                            <controls:BalanceView Grid.Row="1" BindingContext="{Binding Balance}" />
                            <sf:SfSegmentedControl Grid.Row="2"
                                x:Name="StatusFilter"
                                SelectedIndex="{Binding SelectedStatusIndex}"
                                SelectionChanged="StatusFilter_SelectionChanged"
                                HorizontalOptions="Center"
                                Margin="0,4,0,0">
                                <sf:SfSegmentedControl.ItemsSource>
                                    <x:Array Type="{x:Type sf:SfSegmentItem}">
                                        <sf:SfSegmentItem Text="All" />
                                        <sf:SfSegmentItem Text="Pending" />
                                        <sf:SfSegmentItem Text="Approved" />
                                    </x:Array>
                                </sf:SfSegmentedControl.ItemsSource>
                            </sf:SfSegmentedControl>
                            <VerticalStackLayout Grid.Row="3" Spacing="15"
                                Margin="0,0,0,20"
                                BindableLayout.ItemsSource="{Binding Expenses}">
                                <BindableLayout.ItemTemplate>
                                    <DataTemplate x:DataType="models:ChargeDto">
                                        <controls:ExpenseItemView />
                                    </DataTemplate>
                                </BindableLayout.ItemTemplate>
                            </VerticalStackLayout>
                        </Grid>
                    </ScrollView>
                </pullToRefresh:SfPullToRefresh.PullableContent>
            </pullToRefresh:SfPullToRefresh>

            <VerticalStackLayout VerticalOptions="End" HorizontalOptions="End" Spacing="12" Margin="30">
                <Button
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                    Command="{Binding AddPaymentCommand}"
                    BackgroundColor="{StaticResource Primary}"
                    CornerRadius="30"
                    HeightRequest="60"
                    WidthRequest="60"
                    SemanticProperties.Description="Add payment">
                    <Button.ImageSource>
                        <FontImageSource Glyph="{x:Static fonts:FluentUi.money_20_regular}"
                                         FontFamily="{x:Static fonts:FluentUi.fontFamily}"
                                         Size="24"
                                         Color="White" />
                    </Button.ImageSource>
                </Button>
                <controls:AddButton
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                    Command="{Binding AddExpenseCommand}"
                    Margin="0"
                    SemanticProperties.Description="Add expense" />
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>
